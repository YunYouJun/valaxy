# Legacy API redirects

The API reference now lives at <https://valaxy.site/api/> and is generated by `valaxy-addon-typedoc` inside the main Press documentation site.

This directory only preserves the old-domain URL snapshot, redirect artifact and [deployment/rollback instructions](./migration/README.md). The standalone VitePress site has been retired.

```sh
node scripts/build-api-redirects.mjs
```
